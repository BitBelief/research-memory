---
name: lidar-ogm-forecast-spec-v2
description: 現行方向——密集人流下的穩健導航；讓機器人知道自己看不到什麼，並讓那個判斷可被驗證。含環境、指標、第一個實驗。
metadata: 
  node_type: memory
  type: project
  originSessionId: d5a2aa35-3a6f-43a8-ad6e-7e6ef9fff25b
  modified: 2026-09-14T12:54:46.985Z
---

2026-08-28 轉向，取代 [[bev-system-spec-v1]]（雙目相機那版，只留幾何教訓）。

**硬體（使用者已有）**：差速自走車 + **2D 光達**，定位可運作。**光達型號待補**（決定格子大小與取樣頻率）。
雙目相機**擱置**（見 [[user-stereo-camera-hardware]]）。捨棄理由：標定、深度雜訊、3D 壓 2D、近場死區、視野受限，全是為了讓相機做到光達本來就會做的事——**成本高但買不到新意**。

**硬體補正（2026-08-28 使用者更正）**：**已有差速自走車 + 2D 光達，定位可運作**。不需要買硬體、不需要只靠公開資料集。

**⚠⚠ 2026-09-14 光達更正：實際在用的是 S100 原廠的「雷神 M10P」（LSLIDAR / 镭神智能 M10_P），不是 RPLIDAR A1。**
→ 驅動＝`lslidar_driver`（在 `~/wheeltec_ws/src/drivers/`，`robotverseny/drivers` fork，ROS 2 Jazzy）。`lidar_name` 選項含 `M10_P`；`interface_selection: serial`、`serial_port_: /dev/wheeltec_laser`。
→ **下面那整組 A1 實測規格（7.8 Hz／720 點／0.5°／12 m／真實約 512 點）全部不適用於 M10P，必須重測。** 連帶要重算：格子大小與取樣頻率、行人可靠偵測距離、SOGMP 相容性（上採樣倍率）。
**★ `params/lsx10_1.yaml` 三個會咬論文的參數**
1. **`angle_disable_min/max` 預設 0.0/90.0 ＝ 角度裁剪**。「視野外 0.00%」這個關鍵結論的前提是全視野在量 → **錄資料前必須確認語意並設成不裁剪**，否則遮擋成因分解全錯且不會被發現。
2. **`compensation: true`** ＝ 與 A1 的 `angle_compensate` 同類陷阱（內插格點會平滑掉遮擋邊界的距離跳躍）→ 分析遮擋要用未補償的原始量測。
3. **`high_reflection`** 是 M10_P 專屬，註解要求「不確定請聯絡技術支援」→ 要跟 WHEELTEC 確認。
另：`lidar_name` 預設 `M10` 要改 `M10_P`；`pubPointCloud2` 對 2D 光達可關。

**★ 硬體已確認（2026-09-08）**
- ⚠ **以下 RPLIDAR A1 段落已被上方 2026-09-14 更正取代，僅保留方法論教訓（角度補償、規格表不可信、要看驅動實際回報）。**
- **光達＝RPLIDAR A1**（Slamtec，三角測距，360°，USB 轉序列 /dev/ttyUSB*，A1 baud 115200）。驅動：`sllidar_ros2`（Slamtec 官方，原 `rplidar_ros`）。
- **車上運算＝Jetson Xavier NX 16G**。384 核 Volta + 6 核 Carmel、21 TOPS、16GB @ 59.7 GB/s。
- ⚠ **Xavier NX 卡在 JetPack 5.1.x = Ubuntu 20.04**（JetPack 6 只支援 Orin）。原生只有 Foxy/Galactic（皆 EOL）；筆電是 **Jazzy（24.04）→ 版本對不上**。解法：Xavier 跑 Docker + Humble，或 Xavier 只跑驅動、筆電當大腦。跨版本 DDS 互通**未實測**。
- **算力不是瓶頸**：AGX Xavier 跑 200×200 4 層 ConvGRU 有 13.87 Hz，64×64 少 10 倍工作量 → NX 餘裕很大。**不要為了 NX 做模型壓縮**（見下方「輕量化」）。
- **車體＝WHEELTEC S100**，控制介面 `/cmd_vel`，詳見 [[wheeltec-s100-platform]]。

**★ 換 Orin Nano？（2026-09-09 使用者提出，結論：現在不換）**
新事實：**JetPack 7.2 支援 Orin，Ubuntu 24.04 + ROS 2 Jazzy 原生**（Isaac ROS 4.6 同步支援）→ Orin Nano 可與筆電同版，DDS 互通問題直接消失。Xavier NX 仍卡在 JP5.1.x/20.04。⚠ 此資訊來自論壇／第三方教學，下單前須上 NVIDIA 官方頁自行確認。
巧合：**手邊那顆 19V/3.42A 正是 Orin Nano Devkit 原廠規格**（買錯的東西在這條路上是對的）。
成本：Orin Nano **沒 eMMC → 必買 NVMe**；JETSON-IO-BASE-A 不支援 Orin → 要買 Devkit；**最大隱藏成本＝WHEELTEC 驅動（ROS1/Foxy 時代寫的）不保證能在 Jazzy 編過**（退路：協定簡單，自寫最小驅動約一天，但那天從論文時間扣）。
**決策（三階段）**：① 現在不換——第一個實驗純資料集，不需真機 ② 要錄資料時先測四個 topic 能否跨版本通；不通就**兩邊都跑 Humble Docker**（`dustynv/ros:humble-ros-base-l4t-r35.1.0`），同版本、零花費 ③ 有預算或 Docker 走不通才換。
理由與「不要為了 Xavier 做輕量化」同構：**運算平台不是研究變數**。若換，那是實驗室長期資產的決策，不是論文的決策，兩件事分開算。
⚠ **以上「現在不換」已於 2026-09-14 作廢，見下一段。**

**★★ 2026-09-14 決定：改用 Orin Nano，全部運算搬上車**
使用者理由：**筆電放在車上太危險**。
⚠ 誠實的但書（已告知）：安全理由同樣成立於手上的 Xavier NX。買 Orin 的真正理由是 **Python/torch 版本**——NX 卡在 JP5.1/Ubuntu 20.04 → Python 3.8、torch 2.1 上限，而管線是 Py3.10/torch 2.7——加上長期資產。

**補強的系統論證（可寫進論文系統章節）**：筆電留在車下用 WiFi 當大腦的話，**每個控制週期都要過 WiFi**。密集人流場地訊號最差，掉線 >0.2 s 就撞上自訂安全超時（走走停停），最壞情況舊指令被 STM32 看門狗撐到 1 秒＝0.5 m/s 滑行 50 cm。**大腦上車是控制迴路的正確設計，不只是「筆電怕摔」。**

**這個決定刪掉的兩個待辦**
- ❌「先測四個 topic 能否跨版本通」→ 不需要。大腦上車後**沒有跨版本邊界**；筆電退化成開發／訓練／離線分析機，兩邊唯一介面是 **bag 檔**，而 `~/ogm_audit/` 是純 numpy/torch，不經過 ROS。
- ❌「最大隱藏成本＝WHEELTEC 驅動在 Jazzy 編不過」→ **2026-09-09 自行解碼協定成功那天就作廢**。`read_chassis.py`／`motion_test.py` 是純 pyserial，與 ROS 版本、與 WHEELTEC 套件無關，複製即可用。
→ 因此 **JetPack 7 / Ubuntu 24.04 支不支援 Orin Nano 已不是擋路石**，只影響順不順手；JP6/Humble 一樣做得完所有事。下單前仍查官方支援矩陣，但別為了等答案而不下單。

**⚠ 真正的新風險是電源，不是軟體**
底盤電池實測 **25.40 V**，Orin Nano Devkit 官方 **19 V** → **直接接超過輸入上限**，必須降壓。⚠ 輸入範圍自行確認（Xavier NX 世代是 9–20 V，Orin 未證實）。
第二個更陰險：馬達起步／堵轉／急停的**電壓凹陷會讓 Jetson 重開機**，而那會發生在「車在動」那批最貴的資料中間。
→ 對策擇一：① **★ 獨立供電**（PD 行動電源＋誘騙器取 15 V，與馬達完全解耦，最省事）② 從電池降壓，**輸出設 15 V 不要設 19 V**（電池掉到 20 V 時降到 19 V 沒餘裕），電流 ≥5 A。
**⚠⚠ 桌上將同時存在「只吃 5V 的 Waveshare JETSON-IO-BASE-A」與「吃 19V 的 Orin Devkit」，桶狀接頭外觀相同**（內徑可能一個 2.1 一個 2.5）→ **線上貼標籤**。已誤插過一次 19V 進 5V 板。

**採購**：Orin Nano Devkit 8GB（確認 Super 版）／NVMe M.2 2280 500GB（資料率僅 **2–3 MB/分**，買 NVMe 是為可靠度與裝 torch 的空間，不是容量）／**M.2 E-key WiFi 模組＋天線（先確認 devkit 有無附；沒 WiFi 就無法 ssh 監看跑動中的車）**／microSD（先開機驗硬體再刷 NVMe）／**短**USB 線 ×2（長線纏輪子）。

**Day-1 驗收（沿用既有標準，硬體與軟體分開驗）**：① `lsusb`+`dmesg` 確認 CH343→`/dev/ttyACM*`、CP2102（`cp210x`）→`/dev/ttyUSB*` ② 跑 `read_chassis.py 5`，門檻＝ **100 幀／0 校驗失敗／20.0 Hz／加速度 Z≈+1.00 g** ③ `/dev/serial/by-id` udev 固定名稱 ④ `motion_test.py`（輪子架高）⑤ **這四步過了才裝 ROS 與 torch**。

**時程**：硬體**不擋任何待辦**（歸因重跑、SCAND、完整資料集變異、多 horizon、三態重訓對照全是純離線）→ 下單後照常做分析，等貨期間先修 `attribute.py` 的「最近一條光束」缺陷。

**★ A1 實測規格（2026-09-08，驅動實際回報，非規格表）**
掃描模式 **Express**、取樣率 **4 KHz**、掃描頻率 **10.0 Hz**、最大距離 12.0 m、firmware 1.28、hardware 5。
→ **每圈 400 點、角解析度 0.9°**。**10 Hz 跟 SOGMP 一致，時間軸不用換算。**
→ 行人可靠偵測約 5–6 m（0.9° 在 5 m ＝ 7.9 cm，0.4 m 寬的人約 5 點；10 m 只剩 2.5 點）。

**★★ 錄製規格（2026-09-08 實測 `/scan`，非規格表）**
`angle_min=-π`、`angle_max=+π`、`angle_increment=0.0087388 rad (0.5°)`、**`len(ranges)=720`**、`range_min=0.05`、`range_max=12.0`、`scan_time≈0.128 s` → **實際 7.8 Hz**（驅動印的 10.0 Hz 是模式標稱值，不可信）。抖動 ±3.5% → **管線必須重採樣到固定頻率，不能假設 dt 是常數**。

⚠ **720 是 `angle_compensate=true` 補出來的格點，不是真實量測數。** 真實每圈約 **512** 點（4 KHz ÷ 7.8 Hz）→ **真角解析度約 0.7°，不是 0.5°，約 30% 是內插的**。
→ 這會**平滑掉遮擋邊界的距離跳躍**（交大 critical corner 那類偵測會受影響）。分析遮擋時考慮 `angle_compensate:=false` 用原始量測。
⚠ `range_min` 回報 0.05 m，但 A1 規格是 0.15 m（驅動 commit 改的）→ **低於 0.15 m 的回波不要信**。
⚠ **SOGMP 相容性量化**：SOGMP＝270°/1080 束＝0.25°；你在 270° 內只有 540 格（真實約 384 點）→ 要餵它得上採樣約 **2.8 倍**，大半憑空內插。**預訓練權重別指望直接用。**

**⚠ A1 的兩個會咬人的點**
1. **取樣率固定（4 KHz）→ 掃描頻率 × 角解析度是常數**。轉快＝點更稀。10 Hz / 0.9° 是預設也是好的平衡點。
2. **A1（360°）餵不進 SOGMP 預訓練權重**：SOGMP 是 1080 beams / −135°~+135°。要嘛裁切＋重採樣對齊，要嘛重訓。錄自己的資料前先決定。
3. 三角測距**怕陽光**，室外會爛；室內密集人流沒問題。行人可靠偵測距離約 **5–6 m**（非標稱 12 m）——1° 在 10 m 是 17.5 cm 弧長，0.4 m 寬的人只有約 2 個點。

**❌ 已否決：為了 Xavier 做輕量化網路（2026-09-08）**
理由：① 算力有大量餘裕 ② **校準不創造資訊——網路變差＝集合變寬，而緊緻度正是主圖的一條軸**，等於拿要量的東西換不缺的東西 ③ 會多出一整條消融軸，而時間才是瓶頸。
→ 正確的問法是「**網路憑什麼存在**」＝ [[forecast-baseline-comparison]] 的 P1／等速外插＋校準高斯對照。若 baseline 贏，最輕的系統就是**沒有網路**。
→ 要回應「能不能上真車」：**量延遲中位數／P95 填一行表格**，不要改架構。

**★★ 研究定位（2026-08-30 使用者拍板，取代先前版本）**

**題目／發想（使用者自己的話）**：**密集人流下的穩健導航**
**主軸句**：**讓機器人知道自己看不到什麼，並讓那個判斷可被驗證。**

**訴求順序：安全優先，效率是約束**（不是次要目標）——只追求安全就不要動了，難的是取捨，而取捨需要一把尺。
**「穩健」的可測定義**：換場景、換人流密度、遇到遮擋時，機器人對自己判斷的信心**仍然說得算**。（對應跨資料集實驗）

**⚠ 用詞紀律：不要說「保證」。**
保形只給「可交換性下的**邊際**涵蓋率」——換場景就破，而且不保證在人多時也成立。robotics 圈把 guarantee 讀成安全保證（OA-MPC 的地盤）。
→ 一律用「**校準過、可驗證**」。重點不是它一定對，是**它終於能被查**。

**報告時的主從（2026-08-30 使用者修正，重要）**
- **標題／開場＝遮擋**（「模型在看不到的地方很有信心地說空的」）—— 好懂、有圖、有安全後果
- **校準＝工具**，放方法章節。不進標題。
- 護身符句：**「我不是提出三態，我是量這個領域把三態丟掉的代價。」**

**兩篇主角，角色不同：**
| 論文 | 角色 | 回答 |
|---|---|---|
| **Principles and Guidelines**（arXiv 2306.16740, ACM THRI） | **框架** | 為什麼值得做 |
| **SCOPE**（T-RO 2025） | **技術對象** | 現況錯在哪 |

Principles 那篇：**P1 = Safety**（社群共識）。且分類法明列「可見性是否受限於機器人感測器（含 **occlusions**、directionality、sensing delay）還是用 ground truth」——**社群點名了這個維度，但從來沒有把它操作化成指標。** ⚠ 引用前務必自己核對原文措辭（我是從 HTML 版抓的摘要）。

**新穎性不是決定因素（使用者明確表態）**：「別人做過也沒關係，結果比較差也沒關係」。
→ 真正的約束是：① **結果差要解釋得出原因** ② **要知道別人是誰、差在哪** ③ **範圍**（時間才是瓶頸）。
→ 先前那些「這被佔住了／你會被 scoop」降級成**寫論文時的引用問題**，不再當方向選擇依據。

**❌ 仍然不能說的話**：
- 「我提出三態 OGM 預測」→ 1985 年就有，Occ3D 內建
- 「我讓機器人能看穿遮擋」→ 物理上做不到
- 「我保證更安全」→ 你保證的是涵蓋率不是安全
- 「沒有人研究過遮擋」→ 一堆人研究過（OA-MPC、phantom agent）

**方法設計：兩種格子分開處理**
| 格子 | 靠什麼 | 為什麼 |
|---|---|---|
| **觀測過** | 學習式預測 + 保形校準 | 有資料 |
| **未觀測的靜態結構** | （可選）Occupancy Anticipation 那類 | 有建築規律可學 |
| **未觀測的動態物體** | **有記憶的可達性** | **沒有規律，學不出來** |

★ **有記憶的可達性**＝ OA-MPC 的改進點：它假設遮擋邊界隨時可能冒出人，**沒有記憶**。但「兩秒前才看過那裡是空的」會讓可達集合半徑只有 `v_max × (t − t_最後觀測)`。
→ **不是比 OA-MPC 更冒險，是用你確實擁有的資訊縮小最壞情況。** 這需要每格的「最後觀測時間」——可見性遮罩沿時間累積就有。

**分階段**：v1 只做「觀測過的格子有校準涵蓋率＋未觀測明確標示給規劃器」就能寫論文；v2 才加有記憶的可達集合。

**★★★ 第一個實驗已跑出結果（2026-09-09）——比原假設更強**

**發現（比「他們把三態二值化」更嚴重）**：SOGMP repo **本身就有完整三態程式碼** —— `local_occ_grid_map.py` 的 `update()`（Bresenham 射線＋log-odds＋inverse sensor model）、`to_prob_occ_map()`、`calc_MLE()`、`bresenham_torch.py`。**但 `train.py` 與 `decode_demo.py` 一次都沒呼叫它們，是死碼。**
實際跑的只有 `discretize()`：光達端點設 1、其餘全 0 → **沒有射線追蹤、沒有 free/unknown 之分**。那不是佔據網格,是**光達命中圖**。

**⚠ 量化（2026-09-09 修正版,200 幀 × 64×64 = 819,200 格,OGM-Turtlebot2 test）**
❌ 作廢：初版報 unknown=60.00%。**那是高估**——我沿用了 SOGMP 的 `update()`,而它只對「端點落在地圖內」的光束打射線（`end=nonzero(binary_map)`）,穿過地圖未在圖內終止的光束完全不貢獻 free。
✅ 改用**極座標最近光束判定**重算（`~/ogm_audit/decompose_unknown.py`）：

| 修正後三態 | 比例 |
|---|---|
| free | **82.35%** |
| occupied | **1.18%** |
| **unknown** | **16.47%** |

**★★★ unknown 成因拆解——這才是論文的殺手數字**
| 成因 | 佔 unknown |
|---|---|
| ① 視野外 | **0.00%** |
| ② 超出量程 | **0.00%** |
| ③ **遮擋陰影** | **100.00%**（全圖 16.47%）|

為什麼是 0：地圖格子方位角只到 **±89.1°**（FOV ±135°）、最遠格心 **7.09 m**（量程 **20 m**）→ **整張圖都在視野內、量程內**。
→ **16.47% 比 60% 更好用**：每一格都是「看得到的方向、量程之內、單純被擋住」,**沒有一格能被辯解成感測器固有限制**。逐幀範圍約 5.4%~18.0%。

**論文第一段定稿方向**：「平均 16.5% 的格子位於障礙物遮擋陰影中——在視野內、在量程內。其中沒有任何一格超出感測範圍。SOGMP 的 GT 把它們全部標記為 `0`,與『觀測到是空的』同值。」

⚠ **方法論細節（要自己先講）**：用「最接近光束」把格子對應到量測值,近距離一束涵蓋多格、遠距離多束落同格 → **遮擋邊界有約一格誤差**。嚴謹版應取格子角度範圍內所有光束的最小值。

**★ 護身符句升級**：「我不是提出三態。**他們自己寫了三態的程式碼,只是沒有呼叫它。我量的是這件事的代價。**」

**SOGMP 原始參數（一字不改沿用）**：`P_prior=0.5`（→ log-odds 恰為 0,所以「從未被射線碰到」可精確判定）、`P_occ=0.7`、`P_free=0.3`、`TRESHOLD_P_OCC=0.8`、map `X=[0,6.4] Y=[-3.2,3.2]` res `0.1` → 64×64、FOV ±135°、`SEQ_LEN=10`。

**程式碼位置**（使用者自有,與 SOGMP 分開以免授權污染）
- `~/ogm_audit/build_gt.py` — 兩種 GT ＋ 統計,輸出 `out/stats.npz`
- `~/ogm_audit/plot_fig1.py` — 出圖（⚠ matplotlib 需設 `font.sans-serif=['Noto Sans CJK JP',...]` 否則中文變方框）
- 圖：`~/ogm_audit/out/fig1_gt_comparison.png`、`fig1b_composition.png`

**環境重建路徑（2026-09-09 已重建,與舊筆記不同）**
`~/sogmp_audit/`（clone 自 `TempleRAIL/SOGMP` branch **`sogmp`**,預設分支就是它;含 `model/model.pth` 8.5MB）、資料 `~/data/OGM-datasets/`（1.6GB;Turtlebot2 94,890 筆 / test 子集 17,001、Jackal 2,010、Spot 1,890）。conda `sogmp` 未動。

**⬜ 下一步（審查者一定會問,不做會被打）**：把 unknown 拆成 ①視野外 ②超出量程 ③**遮擋陰影（量程內視野內但被擋住）**。③ 才是論文標題講的東西,也是人真的可能站著的地方。**它佔多少會直接決定論文第一段怎麼寫。**

**★★★ Figure 1 右半也完成了（2026-09-09）——reliability，比左半更狠**

150 幀、1 步預測（T=1）、32 個隨機樣本取平均。觀測過 432,621 格 / 遮擋陰影 181,779 格。

**① 遮擋陰影裡模型高度肯定「空的」**
`p<0.05` 佔 **96.46%**、`p<0.10` 佔 **97.10%**、中位數 **0.0000**、平均 0.0175。
→ **在完全沒有觀測資訊的地方,97% 的格子給出接近零的佔據機率。**

**② ★ ECE 陷阱——這本身就是一個發現,直接支撐「現有評估看不出問題」**
整體 **ECE = 0.0074**（按慣例叫「校準良好」）。但分箱拆開,**只要模型真的下判斷就系統性過度自信**：

| 預測 | n | 模型說 | 實際 |
|---|---|---|---|
| [0.0,0.1) | 419,331 | 0.001 | 0.000 ✅ |
| [0.5,0.6) | 549 | 0.550 | **0.264** |
| [0.7,0.8) | 730 | 0.751 | **0.366** |
| [0.8,0.9) | 1,065 | 0.856 | **0.466** |
| [0.9,1.0) | 6,582 | 0.977 | **0.760** |

ECE 漂亮是因為 **97% 的格子落在「說空的、也真的空的」那一格**（實際佔據率僅 **1.53%**）。**極端類別不平衡讓聚合指標整個蓋掉問題**——reliability 曲線全程在對角線下方。

**→ Figure 1 完整了**：左半（`fig1_gt_comparison.png`）證明 GT 錯；右半（`fig2_reliability.png`）證明模型確實學壞了,而且現行指標抓不到。

**程式**：`~/ogm_audit/reliability.py`（⚠ checkpoint 的 state_dict 在 `ck['model']`,不是 `ck['model_state_dict']`）、`plot_fig2.py`。輸出 `out/reliability.npz`。
⚠ 只做了 **horizon=1**。decode_demo 對每個 horizon 會用不同的預測原點重建輸入,不能只 rollout 一次重用；要多步就得逐 horizon 重跑。

**★★★ 走過去驗證 + 跨資料集都跑完了（2026-09-09）**

**方法（不需訓練,純幾何）**：對參考時刻 t 算出遮擋陰影 → 陰影格心換算世界座標 → 往後看 K 幀,每幀把格子轉進當時的機器人座標系查掃描 → 記錄「第一次被看到」的結果。程式 `~/ogm_audit/reveal.py`、`cross_dataset.py`。

**跨資料集結果（n=80,stride=10,K=40）**
| | Turtlebot2 模擬 | Jackal 真實戶外 | Spot 真實室內 |
|---|---|---|---|
| 感測器 | 1080束/30m | 1800束/130m | 897束/200m |
| unknown 佔全圖 | 31.0% | 21.5% | 36.1% |
| **遮擋成因** | **100%** | **100%** | **100%** |
| 陰影被驗證比例 | 40.6% | 100% | 91.8% |
| **驗證後其實有東西** | 7.96% | **22.47%** | **32.65%** |
| **模型平均說** | 2.63% | 2.26% | 3.75% |
| **低估倍數** | 3× | **10×** | **8.7×** |
| **有東西卻說 p<0.1** | 66.5% | **89.2%** | **90.9%** |
| 觀測格 ECE | 0.0063 | **0.0044（最好）** | 0.0223 |

**★★ 第二張圖的殺手論點：ECE 反著走。** Jackal 的 ECE 最漂亮（0.0044）卻在遮擋處錯得最兇（89.2%）。**現行指標不只抓不到問題,還會把最糟的評為最好。**
**★ 缺陷是普遍的,不是模擬產物**：三種感測器、模擬與真實、室內與戶外,**六次測量遮擋成因全部 100%**（視野外 0%、超出量程 0%）。真實資料比模擬嚴重 3 倍。

**★ 意外的好消息（幫論文排除最大風險）**：模型在陰影裡**不是瞎猜**——後來確認有東西 vs 是空的,平均輸出 **0.257 vs 0.012（差 21 倍）**。→ 底下預測器有鑑別力,**校準後會得到「又誠實又有用」的集合,不是寬得沒用的廢話**。這直接反駁了自己筆記裡「校準不創造資訊」的最壞情境。

**⚠⚠ 三個必須修的方法論問題（投稿前）**
1. **★ 數字隨取樣跑掉**：Turtlebot2「驗證後有東西」在 stride=100/n=120 是 **13.72%**,stride=10/n=80 是 **7.96%**——差近 2 倍（stride=10 只涵蓋 800/17000 幀且高度重疊）。**必須用完整資料集跑並報變異範圍。目前數字方向對、絕對值不可靠。**
2. **「靜態鐵證」在真實資料失效**：Turtlebot2 靜態佔 52.7%,Jackal/Spot **0%**。⚠ **這不是真實環境沒有牆,是判定條件（≥3 次觀測且 ≥80% 說有障礙）對真實光達太嚴格**（回波有雜訊；Jackal 0.15° 角解析度下一格含多束而我只取最近一束）。**不修的話「牆不會走路」這個最強論證在真實資料上用不了。**
3. `OGM-Spot/test_union1` 機器人**完全靜止**（0.000 m/幀）,不能做走過去驗證；已改用 `test_union2`。可留作「靜止對照組」。
⚠ 另：Turtlebot2 實際最大量程是 **30 m** 不是我初期假設的 20 m（單一樣本看到的）。結論不受影響——因為地圖只到 7.09 m,任何回報 >7.09 m 的光束都讓整條射線變 free。

**圖**：`fig1_gt_comparison.png`(GT 對照)、`fig2_reliability.png`(ECE 陷阱)、`fig3_reveal.png`(走過去驗證)、`fig4_cross_dataset.png`(跨資料集)。

**★★★ 方法論修正版（2026-09-09 晚）——以下數字取代前面所有版本**

**❌ 舊方法的錯**：用「最近的一條光束」判定格子狀態。真實光達一格橫跨 **3.8~7.6 條光束**（Jackal 0.15°/束,5 m 處 7.6 條）→ 只查一條等於扔掉 87% 的資訊,**系統性漏抓障礙物**。診斷實證：「曾被看到有東西」的格子,在真實資料上平均只有 **10%** 的觀測說它有東西（中位數 ratio：Turtlebot2 0.22 / Jackal 0.10 / Spot 0.11）——靜態判定的條件 A（≥3 次觀測）**100% 通過**,殺手是條件 B。根因：**牆是一個表面,不是「光束落點那一格」**;機器人一動,落點就沿牆滑走。

**✅ 正確做法＝逐光束光柵化**（`~/ogm_audit/raycast.py`）：沿每條光束取樣 → 經過的格子標 free → 真實回波落點標 occupied → 沒被碰到的＝unknown。查詢任意點用 `probe()`：取**格子角寬內的所有光束**,任一落在格內即 occupied。效果：抓到的障礙物約 **2 倍**（Jackal 0.65%→1.55%）。
**同時修正取樣**：參考時刻改成 `np.linspace` **平均散佈整段軌跡**（舊的 stride=10 只涵蓋 800/17000 幀且高度重疊,是 13.72% vs 7.96% 那個 2 倍落差的原因）。

**★ 最終數字（n=150 平均散佈,K=40 幀,`cross2.py` → `out/cross2.npz`）**
| | Turtlebot2 模擬 | Jackal 真實戶外 | Spot 真實室內 |
|---|---|---|---|
| free / occupied / **遮擋陰影** | 66.13 / 1.91 / **31.97%** | 76.03 / 1.93 / **22.04%** | 63.20 / 2.51 / **34.29%** |
| 陰影被驗證比例 | 27.6% | 100% | 93.1% |
| **★ 驗證後其實有東西** | **20.26%** | **42.91%** | **50.79%** |
| └ 靜態鐵證 | **79.11%** | 3.43% | **10.45%** |
| 模型平均說 | 1.67% | 1.33% | 3.45% |
| **低估倍數** | **12×** | **32×** | **15×** |
| **有東西卻說 p<0.1** | 86.04% | **94.91%** | 90.91% |
| 觀測格 ECE | 0.0020 | 0.0068 | 0.0337 |

**★★ 論點更新（比舊版更站得住）**
- ❌ **收回「ECE 反著走」**——那是舊方法的產物。正確關係是**無關**：**ECE 差 17 倍（0.0020→0.0337）,失效率始終卡在 86~95%**。講法改成「**不能用 ECE 預測模型在看不見的地方會不會亂講**」。
- **Spot（真實室內）遮擋陰影裡有一半的格子其實有東西**,模型說 3.45%。
- 靜態鐵證：Turtlebot2 **79%**、Spot **10.45%**（室內有牆有柱,可用）、Jackal **3.43%**——⚠ Jackal 是**戶外開闊**（25% 光束無回波、機器人 2 m/s）,遮擋多來自行人,**靜態少可能部分為真**,但不敢斷言,待查。

**⬜ 仍待處理**：① Jackal 靜態偏低是真是假 ② 用完整資料集跑並報變異範圍（目前 n=150） ③ 多個 horizon（目前只有 T=1）。

**★★★ 自我對抗檢驗四項（2026-09-09 深夜）——以下取代前面所有數字**

**❌❌ 漏洞 A（最嚴重,已修）：參考座標系錯位。** SOGMP 把 GT 放在 `origin_pose_prediction()` 算出的**預測位姿**,但掃描是**實際位姿**量的。實測誤差中位數：Turtlebot2 **0.08 cm**（無害）、Jackal **9.5 cm**、Spot **28 cm ＋ 2.1°**（都超過一格）。
→ 修法：把實際位姿換算進參考座標系,當作 `raycast(origin=...)` 的光達起點（`~/ogm_audit/cross3.py`）。
→ ⚠ **未修正時產生過一個假發現**：Spot ECE 0.0337「模型嚴重過度自信」——**那純粹是座標錯位**。修正後 0.0084,與其他一致。**「ECE 差 17 倍／反著走」的說法一併作廢。**

**❌ 漏洞 B（概念性,改措辭不改數字）：模型輸出的語意。** 訓練是 `nn.BCELoss` 對 `discretize()` → **p ＝「這格會不會是光達回波落點」,不是「有沒有東西」**。牆後面本來就不會有回波,**模型忠實學會了它的目標**。
→ **不能說「模型錯了」**。要說：**目標本身把兩種狀態寫成同一值,無論模型多好都無法表達這個區別**;而下游規劃器讀的就是這個數字。**這個版本無法被「再訓練更好的模型」反駁——資訊根本不在那裡。**

**✅ 漏洞 C（通過,但要改措辭）**：unknown 有 **99.86 / 99.92 / 99.67%** 是真正被擋住;視野外 0.00/0.01/0.26%、角度縫隙 0.14/0.08/0.08%。→ **論文寫「99.7% 以上」,不要寫 100%。**

**✅ 漏洞 D（通過,結論極穩）**：門檻放寬到 **p<0.5** 仍有 **95.0 / 99.2 / 99.0%** 的「確認有東西」格子落在下面。且「低估倍數」完全不涉及門檻。未驗證格子的模型輸出（平均 0.0003~0.0006、99.8%+ <0.1）與已驗證格子無異 → **選擇偏差影響「那裡有什麼」,不影響「模型說什麼」。**

**★ 最終數字（`cross3.py`,n=150 平均散佈,K=40,座標系已修）**
| | Turtlebot2 | Jackal | Spot |
|---|---|---|---|
| free/occ/**遮擋陰影** | 66.14/1.90/**31.96%** | 76.05/1.92/**22.03%** | 63.88/2.59/**33.54%** |
| 被驗證比例 | 27.6% | 100.0% | 92.5% |
| **驗證後有東西** | **20.34%** | **42.90%** | **54.18%** |
| 靜態佔「有東西」 | 79.05% | 3.39% | 10.59% |
| **靜態佔全部驗證格** | **16.08%**（每6格1格）| 1.45% | 5.74% |
| 模型平均說 | 1.71% | 1.35% | 1.22% |
| **低估（無門檻）** | **11.9×** | **31.7×** | **44.6×** |
| 有東西卻說 p<0.1 | 85.94% | 94.77% | 95.67% |
| 陰影格 p<0.1 | 98.93% | 96.45% | 97.23% |
| **觀測格 ECE** | **0.0019** | **0.0076** | **0.0084** |

**★★ 最終論證形式（最乾淨,已排除所有已知混淆）**
> **模型在看得到的地方,三個資料集全部校準良好（ECE 全部 < 0.01）。它只在看不到的地方失效,而且三個資料集一致失效（86~96%）。**
Finding 06 改寫成：**ECE 一律 < 0.01,失效率一律 > 85% —— 指標與問題完全無關**（不是反向,是無關）。遮擋格子根本沒被納入評估,因為現行流程認為它們是 free。

**成果文件（artifact,可隨時開）**：問題定義與發現 https://claude.ai/code/artifact/9d6efc0d-678b-4d82-84fd-4be8c4adeb75 ｜ 路線圖 https://claude.ai/code/artifact/397a82dc-db50-469b-85da-76f7fc289bc2

**⬜ 仍待處理**：① Jackal 靜態偏低（3.39%）是真是假 ② 完整資料集重跑並報變異範圍 ③ 多 horizon（目前只有 T=1）④ 用三態標籤重訓的對照（審查者必問）。

**★ 第一個實驗（不需硬體、不需訓練、資料集現成，幾週內要做完）**
用 SOGMP 的 OGM-datasets + 預訓練權重：
1. 同一份光達資料建**兩種 GT**：(A) 他們的 `discretize()` 二值 (B) Bresenham 三態
2. 量：多少比例的格子被 A 錯標成 free？集中在哪裡？
3. 拿**預訓練模型**在「正確標記」與「錯標」兩群格子上**分別**評估，畫 reliability diagram
4. **預期**：整體看起來還好，**未觀測那一群爛掉** → 那張圖就是論文第一張圖
5. **跨資料集實驗**（第二張圖）：在 Turtlebot2 上定門檻/校準 → 到 Jackal/Spot 量實際涵蓋率。預期他們的固定門檻會飄，保形重新校準能穩住

⚠ **時間壓力是真的**：SCOPE 那組已把遮擋列為「future work」，同實驗室、有 code 有資料有博士生。第一個實驗**幾週內**要有結果。

**成本優勢（小型機器人）**：保形預測的校準是**離線**做的，推論只多一次門檻比較。SCOPE 真機要取 8 樣本、最低 2 FPS。但別當主打，SO-SCOPE 已用查表繞過取樣。
⚠ **校準不會創造資訊**——底下預測器爛，就會得到「校準良好但寬得沒用」的集合。所以緊緻度那條軸不能省。

**flow 是必需品，不是加分項。** 各時刻獨立取門檻會把互斥的未來（往左走／往右走）疊在一起 → 範圍暴脹 → 正是 Trautman 批評的「不確定性爆炸」。flow 提供時間關聯，是唯一解藥。v1「先只用 L_occ」的決定**作廢**。

**實作順序（預測層先做，不需要真機就能完整評估）**
0. 錄製規格 → 1. 資料管線（光達 → 固定頻率 OGM 序列）→ 2. **先實作 baseline**（persistence + 等速外插；等速外插需要的逐格位移**就是 flow**，兩件事同一份工）→ 3. **先寫好評估再訓練**（否則會不自覺調指標去配合模型）→ 4. 才開始訓練。

**錄製紀律**
- 從第一次就同時錄「車停著」與「車在動」。先只處理停著的（無 ego-motion，動靜態＝背景相減）；移動的**先錄起來放著**——現在多錄邊際成本近零，半年後要補得重新召集人重錄。
- 刻意包含線性外插**必然失敗**的情境：走到一半轉彎、突然停／突然起步、兩人交會避讓、進出視野邊緣、互相遮擋。資料太單純的話等速外插贏九成，就量不出差別。

**從 v1 保留仍適用**：分箱與格心公式必須同一組、方位角 raycast 可見性、自監督 GT（未來 occupancy 就是後面幾幀，零人工標註）、flow 由連通元件跨幀配對產生。
**已作廢**：標定、SGBM、高度切片、垂直 FOV 近場死區 `r_near` —— 相機特有的問題。

**★ 評估指標設計（2026-08-30）**

1. **AUPR** —— 只為對照台大 0.57/0.62，當**健全性檢查**
2. **涵蓋率 + 緊緻度** —— 涵蓋率可作弊（說整張圖都可能有人），真問題是**同樣涵蓋率下誰的面積小**。
   ⚠ **baseline 必須也調到相同涵蓋率才能比面積**，否則是稻草人。
3. ★ **急煞率當「被嚇到」的代理指標**（使用者提出，很好用）
   - 碰撞率在實驗中太罕見，**做不出統計顯著性**；急煞事件常發生、好量、**不用真的撞到人**
   - 量：每公里急煞次數、減速度超門檻的事件數、jerk 分布
   - 急煞有兩種成因：**A 規劃器短視**（平滑性成本／加速度約束，用現成的就好，不創新）、**B 世界突然出現沒預期的東西**（＝預測器，而遮擋是最大來源）
   - 邏輯：知道自己看不到 → 提前減速 → **根本不需要急煞**

★★ **權衡曲線（論文的主圖候選）**
```
              信心水準
         80%   90%   95%   99%
急煞事件/km  ←太大膽會高（保守不足＝被嚇到）
不必要減速   ←太保守會高（用台大自己的指標，所以可比）
```
兩條會交叉，**交叉點就是旋鈕該設的地方**。橫軸有單位——**SCOPE（調出來的熵門檻）和 OA-MPC（假設 v_ped 0.6 m/s，真實 1.3）都畫不出來這張圖**。

**環境（2026-08-30 已建好可用）**
- conda env **`sogmp`**：Python 3.10.21 / torch 2.7.1+cu118 / numpy 1.26.4 / matplotlib / tensorboard / tensorboardX / tqdm / scikit-image。GPU 實測通過（RTX 4060 Laptop）
- 啟動：`source /home/user/anaconda3/etc/profile.d/conda.sh && conda activate sogmp`
- code：`/home/user/sogmp_audit/`（SOGMP，branch `sogmp`，含預訓練 `model/model.pth`）
- 資料：`/home/user/data/OGM-datasets/`（Turtlebot2 / Jackal / Spot）
- **已驗證**：權重載入、推論跑通、產出 `output/mask0.jpg`（**那張幾乎全黑的 GT 圖 = 論文第一張圖候選**）
- ⚠ `decode_demo.py` **只畫圖不算指標**，repo 沒附評估程式，AUPR/SSIM 要自己寫
- SOGMP 規格：格子 **64×64 @ 10cm = 6.4×6.4 m（只有前方）**、光達 1080 beams / −135°~+135°、資料是**原始 scans**（資訊沒在資料層遺失，問題純在程式碼）

相關：[[occupancy-forecast-prior-art-2026-08]]、[[taiwan-thesis-prior-art]]、[[forecast-baseline-comparison]]、[[cclstm-paper-spec]]

**★★★ 歸因稽核（2026-09-11）——結論改變論文主張的位置**

前面所有實驗證明的是「GT 錯了、模型學壞了、指標抓不到」。這一輪問的是**不同的問題**：那些被錯標的遮擋佔據，**有多少能被比神經網路更簡單的東西解釋掉**。

**新程式**（`~/ogm_audit/`）：`ab_split.py`（往前看，分「被擋前看過」A / 「從沒看過」B）、`attribute.py`（歸因到靜態累積地圖／等速外插／未解釋，含**虛無對照**）、`unexplained.py`（把未解釋的再拆）。輸出 `out/ab_split.npz`、`attribute_*.npz`、`unexplained.npz`。

**★ 核心結果（OGM-Turtlebot2，n=1,644 個「陰影格露出後發現有東西」事件）**
| 誰解釋得了 | 佔全體 |
|---|---|
| **靜態累積地圖**（因果，只用過去資料） | **69.9%** |
| 暫態佔據（同格過去看過障礙，只是靜態門檻沒過） | 10.6% |
| **★ 學習方法的空間**（過去看過這格是空的，東西移進來，等速外插沒抓到） | **4.5%**（動態部分 **2.2%**） |
| B 型：過去從未觀測過這格 → 保守界領域 | 10.9% |
| 等速外插淨貢獻（扣虛無對照） | 1~2.5% |

**★★ 虛無對照是這輪最重要的方法論收穫。** 等速外插第一次跑出 **14.16%**，把每條軌跡速度方向**隨機打亂、速率不變**再跑得到 **12.67%** → **淨效果只有 1.5%**。那 14% 幾乎全是運氣（94 條軌跡各撐 0.3 m 容差球）。不同容差 0.15/0.30/0.50 與速度過濾 0.3/0.5 m/s，淨效果一律落在 **+0.8%~+2.5%**。
→ **紀律：任何「我的方法解釋了 X%」都要附隨機化對照。** 沒有它這份分析會宣稱高出真值十倍的數字。

**★ A/B 拆解的關鍵趨勢**（`ab_split.py`，歷史 2/4/10 秒）
A1 靜態 27.9%→43.2%→**64.2%**，A2 移動物可追蹤 6.9%→8.1%→**8.5%（飽和）**，B 65.2%→48.7%→27.3%。
→ **記憶拉長吃到的幾乎全是靜態結構，不是被擋住的人。** 人走得快，超過 1~2 秒的記憶對人沒用。

**★★ 決策（門檻在量測前訂好，避免事後合理化）**
≥25% 全力做動態預測 ／ 10~25% 主張收斂成特定情境 ／ **<10% 轉三層融合**。**實測 4.5% → 轉向。**
→ 主張從「預測被遮擋的行人」改為「**靜態累積層 × 動態預測層 × 未知保守界的三層融合與分層校準**」。動態預測保留成一個模組，不是主軸。這與既有的「兩種格子分開處理」「有記憶的可達性」完全相容，只是把權重挪對。
→ 好處：涵蓋 100% 的情況而不是 4.5%；且「分層校準」有第 07 節的實證（整體 ECE 0.0074 漂亮，`p>0.5` 的 ECE 是 **0.262**，96.93% 樣本擠在最低 bin 把問題洗掉）。

**⚠⚠ 必須修才能定稿：`attribute.py` 用的是「最近一條光束」判定，就是自己筆記裡已判定作廢的那個方法**（一格橫跨多束，系統性漏抓障礙）。應改用 `raycast.py` 的 `probe()` 重跑。預期方向：事件數變多；「靜態地圖 70%」因為走端點累積、與此無關，應該穩；**4.5% 這個數字可能移動**。修正前不要對外引用絕對值。

**⬜ 下一步**：① 用 `raycast.probe()` 重跑歸因 ② 在 SCAND（Jackal/Spot）上重跑——真實資料 GT 錯誤率是模擬的 3~4 倍，比例很可能不同 ③ 自有 S100 平台補一段資料 ④ 主張改寫成三層融合。

**成果文件（artifact）**：遮擋歸因稽核 https://claude.ai/code/artifact/01f8d1ed-0313-45b8-9674-793014015372

**★★★ 物件層級直接檢驗（2026-09-11 續）——「記憶撐得了多久」**

格子層級的歸因之外，改做物件層級。`~/ogm_audit/coast.py`：全序列追蹤動態團塊，軌跡斷掉時讓它「滑行」，物體重新出現時量猜測誤差。**13,392 次消失又出現**（速度 ≥0.3 m/s）。

**命中率（誤差 <0.5 m ≈ 一個人的寬度）**
| 遮擋時長 | 等速外插 | 假設不動 |
|---|---|---|
| 0.2–0.3s | **92.0%** | 84.8% |
| 0.4–0.6s | 38.7% | 42.0% |
| 0.7–1.0s | **14.7%** | 19.7% |
| 1.1–1.5s | 6.6% | 9.3% |
| 2.3–3.0s | 2.0% | 5.4% |

**★ 誤差成長速度 ≈ 人走路的速度**（擋 1 秒→誤差 1.37 m，人走 0.97 m）→ **記憶帶來的資訊量趨近於零**。且 **>1 秒後等速外插輸給「假設他站著不動」**（人早就轉彎或停了）。人中位速率 0.97 m/s。圖：`out/fig5_coast.png`。

**★★ 同一組事件換 SOGMP（`model_vs_memory.py`，n=1,930，horizon=gap，p>0.3 且距真實位置 <0.5 m 算命中）**
原始數字 0.7–1.0s **77.0%**，看似大勝等速外插的 14.7%。**兩組對照把它拆了**：
| | 0.2–0.6s | 0.7–1.5s | 1.6–3.0s |
|---|---|---|---|
| 模型命中 | 85–95% | 69–77% | 55–61% |
| 陰影裡隨機挑一點 | 28% | 26–31% | 19–21% |
| 淨（全部事件） | +57~67 | +44~46 | +36~40 |
| └ 人從**靜態結構旁**出現 | +67.5 | +60.1 | +56.3 |
| └ **遠離靜態結構（純動態）** | **+60.6** | **+12.1** | **−7.5** |

→ **77% 幾乎全來自模型正確畫出了牆**，人剛好從牆邊出來。純動態事件下 1 秒處淨效果只剩 **+12**，1.6 秒以上**低於隨機**。模型的「預算」全圖僅 2.10% 格子 p>0.3（所以不是亂塗）。
→ **結論：網路確實贏等速外插，但優勢集中在 0.6 秒以內。** 與格子層級的 4.5% 用完全不同方法互相印證，不是矛盾。

**★★ 兩次「差點報錯」的共同教訓**：等速外插 14%→淨 1.5%（隨機方向對照）、模型 77%→淨 12%（靜態結構對照）。**每一個亮眼數字都要問「隨機會拿到多少」。** 這條紀律要寫進論文的評估協定，也是審查者一定會問的。

**⚠ `coast.py` / `model_vs_memory.py` 同樣沿用「最近一條光束」判定**（已知漏抓障礙）。影響事件絕對數量，不影響各組對照間的相對關係。
